<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".OpeningScreen" >

    <TextView
        android:id="@+id/app_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="40sp"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="50dp"
        android:text="@string/app_name" />

    <ImageView
        android:id="@+id/brand_logo"
        android:layout_below="@+id/app_name"
        android:layout_width="108dp"
        android:layout_height="102dp"
        android:layout_centerHorizontal="true"
        android:src="@drawable/sound"
        android:layout_gravity="center"
        android:layout_marginTop="40dp"
        android:layout_marginBottom="40dp" />

    <Button
        android:id="@+id/opening_button_user_existing"
        android:layout_below="@+id/brand_logo"
        android:layout_alignParentLeft="true"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:text="@string/opening_button_user_existing" />

    <Button
        android:id="@+id/opening_button_user_new"
        android:layout_below="@+id/brand_logo"
        android:layout_alignParentRight="true"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:text="@string/opening_button_user_new" />

</RelativeLayout>